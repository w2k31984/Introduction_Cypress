import type * as logger from '@applitools/logger';
import { LogHandler, LogHandlerData } from './input/LogHandler';
export declare class Logger implements logger.Logger {
    private _logger;
    private _handler;
    private _show;
    private _label;
    private _makeLogger;
    /** @internal */
    readonly isLogger = true;
    /** @internal */
    constructor(logger?: logger.Logger);
    constructor(options?: {
        show?: boolean;
        label?: string;
        handler?: LogHandler;
    });
    constructor(show?: boolean);
    /** @internal */
    get console(): logger.Printer<{
        color: string | string[];
    }>;
    getLogHandler(): LogHandlerData;
    setLogHandler(handler: LogHandler): void;
    verbose(...messages: any[]): void;
    log(...messages: any[]): void;
    warn(...messages: any[]): void;
    error(...messages: any[]): void;
    fatal(...messages: any[]): void;
    open(): void;
    close(): void;
    extend(options?: Omit<logger.LoggerOptions, 'handler'>): Logger;
    extend(label?: string): Logger;
}
